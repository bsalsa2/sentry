# Copy to ".env" for local development.
#
# Leave this blank during local development: vite.config.js already forwards
# /api to the Flask backend on port 5000.
#
# On Vercel, set this to your deployed backend, e.g.
#   VITE_API_URL=https://sentry-backend.replit.dev
VITE_API_URL=
